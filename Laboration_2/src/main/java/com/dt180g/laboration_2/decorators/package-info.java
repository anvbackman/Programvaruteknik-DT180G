/**
 * Package contains the abstract Operative class which is a base decorator used for storing a reference to the previous
 * component.
 * The package also contains the Spy class which represents the regular Spy which is used to transfer messages and increase
 * the depth of the encryption but can not read said messages.
 * The package contains the SpyMaster class which can decipher the messages sent by reversing the encryption.
 */
package com.dt180g.laboration_2.decorators;